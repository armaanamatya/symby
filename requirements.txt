# AI Research Impact Observatory
# DGX Spark Frontier Hackathon - Symby AI Track

# Core data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0

# FastAPI server
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# GPU-accelerated (RAPIDS) - install via conda:
# conda install -c rapidsai -c conda-forge -c nvidia rapids=24.10
# cudf-cu12
# cuml-cu12
# cugraph-cu12

# Web dashboard
streamlit>=1.29.0
plotly>=5.18.0

# Statistical analysis
scipy>=1.11.0

# Graph processing (CPU fallback)
networkx>=3.2

# Utilities
tqdm>=4.66.0
orjson>=3.9.0

# HTTP requests (for Semantic Scholar API)
requests>=2.31.0

# LLM integrations (for validation agent)
openai>=1.0.0
anthropic>=0.18.0

# Docker SDK for sandbox execution
docker>=7.0.0

# Code extraction from papers (pce module)
aiohttp>=3.9.0
PyMuPDF>=1.23.0
GitPython>=3.1.40
